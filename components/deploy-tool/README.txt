To Run the command line tool:

java -jar target/jetspeed-deploy-tools-2.0-a1-dev-uber.jar source.war destination.war

To make a portlet application register at Servlet init time:

java -jar target/jetspeed-deploy-tools-2.0-a1-dev-uber.jar -r source.war destination.war
